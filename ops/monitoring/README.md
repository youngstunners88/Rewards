# Monitoring

The app is fully client-side. There is no server to monitor.

For production health, watch Vercel's deployment dashboard:
- Build status
- Edge function errors (we don't use any today)
- Bandwidth / request volume

If a backend is ever added, document health-check endpoints here.
